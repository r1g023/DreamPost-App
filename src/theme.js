import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "green",
      light: "#800080",
      dark: "#4b0082",
    },
    secondary: {
      main: "#f44336",
    },
    otherColor: {
      main: "#ff9800",
    },
    customColor: {
      main: "#fafad2",
    },
  },
});
