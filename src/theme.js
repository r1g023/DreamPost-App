import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#5a4fcf",
      light: "#800080",
    },
    secondary: {
      main: "#f44336",
    },
    otherColor: {
      main: "#ff9800",
    },
    customColor: {
      main: "#fafad2",
    },
  },
});
